import styles from "./styles.module.scss";

const Section13 = () => {
  return (
    <section className={`${styles.section}`}>
      <h1>Terminy gonią i jesteś już zmęczony/a szukaniem błędów w swoim tekście?</h1>
      <p className={`${styles.caption1}`}></p>
    </section>
  );
};

export default Section13;
